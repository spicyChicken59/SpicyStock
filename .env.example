# Copy this file, don't edit it in place — .env is gitignored, this one isn't.
#
#     cp .env.example .env
#     set -a; . ./.env; set +a
#
# Quote every value in single quotes. That second line sources .env as a shell
# script, so an unquoted value gets executed or mangled — and a value that is a
# syntax error (an unquoted `Alerts <a@b.com>`) aborts the load at that line,
# leaving every variable below it unset while still exiting 0. For a literal
# apostrophe inside a value, close-escape-reopen: PASS='it'\''s'
#
# In GitHub Actions these come from repository secrets, not this file.
# evening.yml forwards all of them except CLAUDE_MODEL, which is local-only.

# --- Required -------------------------------------------------------------
# These fail in three different ways, none of them convenient:
#   ALPACA_*         aborts the run immediately in get_clients() with
#                    "ValueError: You must supply a method of authentication".
#   RESEND_API_KEY   the worst case. Read with os.environ[...] in send_email(),
#   EMAIL_TO         which runs last, so a real run does the entire scan and
#                    every Claude call, then dies on KeyError and mails nothing.
#                    The CSV survives — archive() runs before the email.
#   ANTHROPIC_API_KEY  degrades silently. The run finishes and emails checklist
#                    scores — "AI unavailable; checklist score N/6", risk
#                    "not AI-reviewed" — in place of AI ones.

# Alpaca (https://alpaca.markets -> Home -> API Keys).
# Use PAPER keys. get_clients() pins the TradingClient to paper=True, and
# get_universe() is its only consumer, so live keys fail on a full-universe
# scan. The data client has no paper concept, so with --tickers (which skips
# get_universe) live keys do work — don't rely on that.
# A free Alpaca plan serves IEX data, roughly 2-3% of consolidated volume, and
# no feed= is set anywhere, so the plan default applies. Against the scanner's
# 5,000,000-share floor that matches almost nothing — expect an empty
# shortlist until the threshold is made relative.
ALPACA_API_KEY=''
ALPACA_SECRET_KEY=''

# Anthropic (https://console.anthropic.com -> API Keys). Needs billing enabled.
# Read implicitly by anthropic.Anthropic() in scorer._client().
ANTHROPIC_API_KEY=''

# Resend (https://resend.com/api-keys). Not needed for --dry-run.
RESEND_API_KEY=''

# Recipients, comma-separated. Not needed for --dry-run.
EMAIL_TO='you@example.com'

# --- Optional locally, but set it in CI -----------------------------------

# Sender. send_email() falls back to onboarding@resend.dev, which Resend only
# delivers to the account owner. That fallback never fires in CI: evening.yml
# always sets RESEND_FROM, so an unset repository secret arrives as '' rather
# than as absent. Set a verified-domain sender to mail anyone else.
# RESEND_FROM='alerts@yourdomain.com'

# Scoring model. scorer.py defaults to claude-sonnet-4-6. Local only —
# evening.yml does not forward this, so CI always runs the default.
# CLAUDE_MODEL='claude-sonnet-4-6'

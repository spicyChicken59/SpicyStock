# Test-only dependencies. Install with:  pip install -r requirements-dev.txt
#
# The pipeline's own runtime dependencies stay in requirements.txt; this file
# pulls them in so one command sets up a machine that can run the suite.
# .github/workflows/tests.yml installs this file and nothing else.
-r requirements.txt

pytest>=8.0

# Exercise Anthropic's real HTTP transport without opening sockets.
httpx2>=2.12

# tests/test_workflows.py parses .github/workflows/*.yml and asserts on the
# PARSED structure, because what went wrong there twice was a structural
# property of an `if:` condition and not a spelling. It is here rather than in
# requirements.txt because nothing the pipeline runs reads YAML.
#
# It was missing, and the sandbox this was written in happens to have PyYAML
# installed while the runner does not -- so four tests passed locally and
# errored in CI for three commits. Same lesson as the interpreter difference in
# CLAUDE.md: what is on this machine is not what CI has.
PyYAML>=6.0

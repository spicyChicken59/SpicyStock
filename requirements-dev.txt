# Test-only dependencies. Install with:  pip install -r requirements-dev.txt
#
# The pipeline's own runtime dependencies stay in requirements.txt; this file
# pulls them in so one command sets up a machine that can run the suite.
# .github/workflows/tests.yml installs this file and nothing else.
-r requirements.txt

pytest>=8.0
